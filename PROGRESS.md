# Builder Channel CRM — Progress Tracker

This file records **what has changed and what is still pending**. The detailed task checklist lives in
[`BUILD_PLAN.md`](./BUILD_PLAN.md); this file is the high-level status board and history.

| | |
|---|---|
| **Last updated** | 2026-09-25 |
| **Current phase** | Building — Milestone D (Business Insight) in progress: M09 done, M10 next |
| **Current module** | **M10 — Dashboards, Reports & Analytics** (next) |
| **Overall progress** | 162 / 183 tasks (89%) |

**How to update (after every piece of work)**
1. Tick the finished task IDs in `BUILD_PLAN.md`.
2. Update the module row in the **Module Status Board** (status, done/total, dates).
3. Add an entry at the top of the **Change Log** (date, module, task IDs, summary, commit/PR).
4. Record any new decision in the **Decision Log** and any new question in **Open Questions**.
5. Update the header (last updated, current module, overall progress).

Status legend: ⬜ Not started · 🟨 In progress · ✅ Done · ⏸️ Blocked · 🔁 Needs rework

---

## 1. Module Status Board

| # | Module | Status | Tasks done / total | Started | Completed | Notes |
|---|---|---|---|---|---|---|
| M01 | Project Foundation & Platform Core | ✅ Done | 28 / 28 | 2026-09-24 | 2026-09-24 | Proposed defaults for Q-13–Q-15 adopted provisionally; CI workflow verified locally, first GitHub run on the next PR |
| M02 | Identity, Access Control & Team Structure | ✅ Done | 21 / 21 | 2026-09-24 | 2026-09-24 | Q-04 default (multi-level hierarchy) adopted, D-017 |
| M03 | Builder & Project Management | ✅ Done | 14 / 14 | 2026-09-24 | 2026-09-25 | Catalogue decisions D-018 → D-020 |
| M04 | Lead Management Core | ✅ Done | 23 / 23 | 2026-09-25 | 2026-09-25 | Q-01/02/03/05 defaults adopted (D-021); decisions D-022 → D-028, ADR 0005 |
| M05 | Lead Assignment, Reassignment & Team Workload | ✅ Done | 13 / 13 | 2026-09-25 | 2026-09-25 | Q-06/Q-08 defaults adopted (D-029); decisions D-030 → D-031 |
| M06 | Notifications & Reminders Engine | ✅ Done | 13 / 14 | 2026-09-25 | 2026-09-25 | M06-13 web push (could-have) deferred (D-037); decisions D-032 → D-037, ADR 0006 |
| M07 | Calls, Follow-ups & Callbacks | ✅ Done | 19 / 19 | 2026-09-25 | 2026-09-25 | Q-07/Q-08 defaults adopted (D-038); decisions D-039 → D-043 |
| M08 | Site Visits, Revisits, Bookings & Closures | ✅ Done | 15 / 15 | 2026-09-25 | 2026-09-25 | Q-09/Q-10/Q-16 defaults adopted (D-044); decisions D-045 → D-051 |
| M09 | Billing, Commission & Profit/Loss | ✅ Done | 16 / 16 | 2026-09-25 | 2026-09-25 | Q-11/Q-12 defaults adopted (D-052); decisions D-053 → D-057 |
| M10 | Dashboards, Reports & Analytics | ⬜ Not started | 0 / 20 | – | – | Needs Q-08, Q-10 |
| | **Total** | | **162 / 183** | | | |

**Release milestones**

| Milestone | Modules | Status |
|---|---|---|
| A — Foundation | M01–M02 | ✅ |
| B — Lead Operations MVP | M03–M05 | ✅ |
| C — Sales Execution | M06–M08 | ✅ |
| D — Business Insight (v1.0) | M09–M10 | 🟨 (M09 ✅) |

---

## 2. Pending — Up Next

1. **M10 — Dashboards, Reports & Analytics** (M10-01 → M10-20): metrics service, role dashboards, funnel, source,
   team and project reports, exports. Q-08 and Q-10 defaults are adopted (D-038, D-044). Billing reports from M09
   (P&L, lost opportunities, receivables) join the reports hub.
2. Open a pull request so the GitHub Actions workflow runs for the first time (all its steps were executed locally).
3. Business confirmation still wanted for Q-01 → Q-12 and Q-16 (defaults implemented and configurable).

---

## 3. Change Log

Newest first. One entry per meaningful change (feature, fix, refactor, decision, docs).

| Date | Module | Task IDs | Change | Commit / PR |
|---|---|---|---|---|
| 2026-09-25 | M09 | M09-16 | Full E2E run after M09: 58 passed, 2 failed — the console-error sweep (now ~60 pages) exceeded the 60 s test timeout in development, where each page compiles on first visit (timeout raised to 5 min for that test), and the catalogue test failed once in the full run but passed on two reruns (not reproduced; watch it). Rerun of both on desktop and tablet: 12 passed. Production build and worker bundle succeed. | `test(M09)` |
| 2026-09-25 | M09 | M09-01 → M09-16 | **M09 complete — billing, commission & profit/loss.** Invoice PDF (React-PDF: seller and bill-to, lines with SAC, CGST/SGST or IGST, total in words, bank details, terms, signatory; stored once per issued invoice, watermarked for drafts and cancelled ones) at `/api/billing/invoices/[id]/pdf`, e-mail to the builder with the PDF attached (queued, reply-to the billing e-mail; sent-to and time shown on the invoice), Download / E-mail buttons. Billing now uses the organization's fiscal year (the duplicate billing setting was removed). Q-11 and Q-12 defaults adopted (D-052); decisions D-053 → D-057. **Verified:** lint, format, typecheck, 282 unit + integration tests (22 billing integration tests: Accounts role, permission isolation, rate-card validation and deactivation, commission snapshot from builder vs project slab cards, costs and override reason, booking edits vs confirmed deals, unlock history, cancellation, tax split and rounding, fiscal-year numbering, billable deals, payments with TDS / overpayment / void / cancel rules, dashboard and ageing, P&L by project and executive with business expenses, cost per lead, lost opportunities, overdue alert and digest, CSV/XLSX export, PDF storage and e-mail with attachment; amounts in words and billing maths unit tests), E2E (2 new: booking → commission → invoice → payment → e-mail in Mailpit → PDF → register export; managers and executives see no billing pages, links or exports; M09 pages in the console-error sweep), browser walkthrough and PDF checked page by page. | `feat(M09)` |
| 2026-09-25 | M09 | M09-02, M09-06, M09-07, M09-09 → M09-15 | **M09 UI — billing pages.** `/billing` dashboard (billed, collected with TDS, outstanding, overdue, ageing bars, by builder/month/project; backfill prompt for closed bookings without financials), `/billing/deals` (+ detail: figures, live-total editor with commission override and expense lines, confirm / unlock with reason / recalculate, change history), `/billing/invoices` register, `/billing/invoices/new` and draft editing (billable deals of the builder, other lines, intra-state switch, tax preview), invoice view as issued (seller/bill-to snapshots, lines, CGST/SGST or IGST, bank details, terms) with issue, cancel, record/void payment; `/billing/payments` collections register, `/billing/expenses` ledger with cost per lead; `/reports/profit-loss` (group by builder/project/executive/manager/month, drill-down to deals, organization profit after overheads) and `/reports/lost-opportunities`; `/settings/billing` and `/settings/commission` (percentage/flat/slab cards). CSV/Excel downloads via `/api/billing/export`. Deals gain the `booking.detail.panel` extension point; billing adds a deal-financials card to the booking page. The Accounts role also gets `builders.view` and `projects.view` (report filters); `listProjectOptions` returns `builderId`. Browser walkthrough: rate card → backfill 3 deals → edit costs → draft, issue INV/2026-27/0001 → part payment with TDS; P&L and dashboard figures checked by hand. | this commit |
| 2026-09-25 | M09 | M09-01, M09-03 → M09-05, M09-16 (services for M09-02, M09-06 → M09-15) | **M09 services — billing, commission & profit/loss.** Schema + migration `m09_billing` (commission rate cards; deal financials 1:1 with a closed booking, with expenses and change history; invoices with lines, tax lines and party snapshots; payments with TDS and voids; business expenses). Permissions `finance.view|manage`, `billing.view|manage`, `commission.manage` (admins only by default) and an **Accounts** role offered once per organization (all billing permissions, all bookings with values). Commission engine: project card before builder card, validity on the booking (or closing) date, percentage / flat / slabs by value or yearly volume, snapshotted on `booking.closed`, recalculated from booking edits while draft, locked on confirm, unlocked only with a reason and never while billed. Invoices per builder: drafts without numbers, issue numbers them per fiscal year (`INV/2026-27/0001`), CGST+SGST split or IGST (remainder on the second line), due date from payment terms, confirms the deals billed; cancelled never deleted; payments amount+TDS ≤ balance drive Issued → Partly paid → Paid, voids reverse. Reports: billing dashboard (billed/collected/outstanding/overdue, ageing buckets, by builder/project/month), P&L by builder/project/executive/manager/month less business expenses, lost opportunities by reason/executive/project/builder, collections register, cost per lead by source; CSV/XLSX export of the registers (spreadsheet writing moved to `platform/export`). Overdue-invoice alert and a billing digest block (digest lines gain an optional `display` for amounts). 20 integration tests. | this commit |
| 2026-09-25 | M08 | M08-01 → M08-15 | **M08 complete — site visits, revisits, bookings & closures.** Schema + migration (`site_visits` — project and its builder, visit/revisit number, the visit a revisit follows, time, executive, pickup, people coming, notes, confirmation, outcome, feedback, who went, no-show, cancellation and the reschedule chain; `visit_outcomes` with category and suggested next step; `bookings` — BK number, lead, project/builder, the visit it followed, credited executive and their manager at the time, customer and co-applicant, tower/unit/floor, configuration, carpet area, booking date, agreement value and token amount (decimals), payment plan, builder reference, status in progress / closed-won / cancelled, current stage, closure and cancellation with reason; `booking_stages`, `booking_history`, `booking_files`; `loss_reasons` for lost / not interested / booking cancelled; lead milestones `firstVisitAt`, `bookedAt`, `lostAt`, `lossReasonId`, with `closedAt` now meaning won — existing rows migrated). Seeded 6 visit outcomes, 9 loss reasons and 3 stages (D-044). **Visits:** plan from the lead page (the lead's projects first; another project joins its interests), weekend quick picks, pickup and people coming; the lead moves forward to Visit / Revisit (D-045); a reminder through M06 two hours before (setting) and a notice when someone plans one for you; confirm, record the outcome with the customer's feedback and who went — then a “what's next?” prompt (plan a revisit, schedule a follow-up, convert to booking, close the lead; the outcome's suggestion first) — no-show, move (history and number kept), cancel with a reason; visits recorded afterwards up to 30 days back (D-046); upcoming visits follow a reassigned lead (D-051). **Bookings:** “Book” / “Convert to booking” form with documents (uploaded straight to storage); booking number, first active stage, lead → Booking, first-booking milestone; the executive's manager and admins are told; booking page with progress (stages → Closed / Won), unit, customer, payment (values only with `bookings.view_value`), documents and the full history; edit with a note (every change historized, value changes hidden from others, D-049), move stage, close as won (lead → Closed / Won), cancel with a reason — the lead becomes Lost with that reason or goes back to an active status (D-050). **Closures:** Mark lost / not interested dialog (reason + note); a loss reason that fits the status is required on every path — status dialog, bulk change, call dialog — through new leads extension points `lead.status.changing` (server hook) and `lead.status.fields` (client field); automation never closes a lead without one; upcoming visits are cancelled, an open booking must be cancelled first, reopening clears the loss (D-047, D-048); the loss reason shows beside the status on the lead. **Screens:** lead Visits and Bookings tabs and header actions (Schedule visit, Book, More → Convert to booking / Mark lost), `/visits` (list with date, status, visit/revisit, builder, project and executive filters; week calendar; team table per executive with drill-down), `/bookings` (date, status, stage, builder, project, manager, executive; totals for value viewers), a “Site visits” tab in My agenda (new activities extension point `agenda.section`), Settings → Visits & bookings (visit outcomes, loss reasons, booking stages with order, reminder / outcome-due / transfer options), lead list filters (site visit, revisit, booking, closure, loss reason) and ready-made views (new `lead.list.preset`: visits waiting for an outcome, visited not booked, closed / won, lost, not interested). Notifications: visit planned for me, a visit's outcome is missing (per visit) and the team's visits without outcome (daily, M06 alert rule), bookings made / closed / cancelled; a “Site visits & bookings” block in the daily summary. `DuePicker` and `ChoiceChips` moved to shared components; saved views now keep other modules' filters; lead status changes no longer run parallel queries on one transaction. **Found while verifying:** booking history showed raw numbers in an unstable order (now formatted and in form order), a “Confirmed” button read like a state (now “Confirm”), and M04's E2E test closed a lead as lost without a loss reason (updated). **Verified:** lint, format, typecheck, 250 unit + integration tests (16 new for M08: seeds, status automation and reminders, scope, the revisit chain with numbering / reschedule / no-show / cancel, forward-only status, transfer on reassignment, booking creation and value permissions, history, stages to Closed / Won with milestones and notifications, the three cancellation paths, loss reasons from the status dialog / mark-lost / call dialog / automation, masters, list filters, alerts and digest; M04/M05 tests updated for loss reasons), 58 E2E tests (4 new, M08 pages in the console-error sweep), production build, worker bundle boots with the new handlers, browser runs as executive, manager and admin on desktop and tablet. | `feat(M08)` |
| 2026-09-25 | M07 | M07-01 → M07-19 | **M07 complete — calls, follow-ups & callbacks.** Schema + migration (`call_logs` — each call its own record with direction, time, duration, reached, outcome, notes, recording, provider/call id; `call_outcomes` master with category, "customer reached", suggested status and "next step required"; `follow_ups` with type follow-up/callback, owner, due time, purpose, notes, status scheduled/done/missed/cancelled/moved, completion, the call that scheduled or completed it and the reschedule chain; `follow_up_purposes`; call summary on the lead: last call, last reached, unanswered calls in a row, last outcome, next follow-up). Seeded 11 outcomes and 6 purposes. **Log call** in one dialog: dial button (`tel:`), outcome chips (reached / not reached), duration, notes, the follow-up it took care of, the lead status pre-selected by the automation rules (outcome mapping → first time reached = Contacted → N unanswered calls in a row = Unresponsive; never on closed, visit or booking leads; workflow statuses still need the override permission when picked by hand) and the next follow-up/callback with quick time picks — all saved in one transaction with timeline, audit and `call.logged`. Outcomes can require a next step. **Follow-ups & callbacks**: schedule from the lead or the call, done (optionally with the next one), move (old item stays as "moved", the new one links back), cancel with a reason; always owned by the lead's owner, moved with the lead on (re)assignment by an event handler with reminders re-targeted (setting), waiting without owner while the lead is unassigned; reminders through M06 before the due time; missed after a grace period (job every 5 min, owner notified, still open until done); the owner is told when someone else schedules one. **Screens:** lead Calls and Follow-ups tabs (recording upload and audited playback for `calls.recordings.listen`), My agenda (overdue / today / upcoming, this week, calls and done today, Call / Log call / Done / Move / Cancel on each item), Calls list (date, outcome, caller, direction, reached; summary), Team follow-ups board (overdue, due today, next 7 days, missed, done today per person with drill-down), Settings → Calls & follow-ups (outcomes with status mapping, purposes, grace period, unresponsive threshold, transfer and automation options), lead list filters (follow-up overdue/today/upcoming/none, callback pending, never called/reached, N+ unanswered, last outcome) through a new leads extension point `lead.list.filter`, and a sortable "Next follow-up" column with overdue highlighting; `tel:` links in the list. Manager alert "overdue follow-ups in my team" and a follow-ups block in the daily summary through the M06 extension points; telephony provider interface with the manual dialer and a webhook stub (`/api/v1/telephony/<provider>`). **Found while verifying:** chip radios were not clickable by automation (now the real input covers the chip), a "Call <name>" link label clashed with the lead link's name, a time-dependent selector in the M03 catalogue test (toasts are list items too) and an order-dependent lookup in the M06 test. **Verified:** lint, format, typecheck, 234 unit + integration tests (19 new for M07: 4 for the status rules, 15 integration — disposition, status automation, follow-up chain, missed job, transfer and reminder re-targeting on reassignment, list filters and sort, scopes, recordings access), 54 E2E tests (5 new), production build, worker bundle, browser runs as executive, manager and admin incl. recording playback from storage. | `feat(M07)` |
| 2026-09-25 | M05 | M05-06 | **Fix:** Settings → Lead assignment → Options read the "unworked after (hours)" field when the form is submitted, so a value typed before the page finished loading is saved instead of the old one (same issue found in M06's settings form); the browser now validates the 1–720 range. Verified in the browser and with the assignment E2E tests. | `fix(M05)` |
| 2026-09-25 | M06 | M06-01 → M06-12, M06-14 | **M06 complete — notifications & reminders engine** (M06-13 web push deferred, D-037). Schema + migration (`notifications` with type/category/deep link/entity/priority/channels/idempotency key/group count/read, `notification_deliveries` with status/attempts/last error, `notification_preferences` member × type × channel, `scheduled_reminders`, `announcements`, `announcement_reads`). **Engine:** notification types declared in module manifests (`notification.type`: category, default channels, essential, manager-only, e-mail button); `notify()` applies organization settings then personal preferences, drops inactive members, stores the in-app notification and queues the e-mail delivery in the caller's transaction, sends each idempotency key once and **groups bulk changes** (one bulk assignment/import/hand-over = one counting notification per person, e-mail delayed 45 s); e-mail delivery job with retries, attempt/error tracking and a daily-summary template; **reminder scheduler** (`scheduleReminder` moves by key, `cancelReminder`, delayed job checks `fireAt`, 5-minute recovery sweep, atomic claim → sent once); **event wiring**: new owner (in-app + e-mail within seconds), previous owner, the owner's manager (manual changes only), original owner of a possible duplicate, importer of a finished/failed import (new `lead.import_failed` event; import events now carry the importer and file name) — nobody is told about their own action; **manager alerts** framework (`notifications.alert-rule`, hourly, 09:00–20:00 local, once a day) with the built-in unworked-leads rule; **daily summary** (`notifications.digest-section`, per recipient scope, organization time zone, configurable time). **Screens:** bell with unread count in the top bar (30 s polling + on focus, dropdown with the latest, opens deep links, mark all read) through a new shell extension point `app.header.action`; `/notifications` (all/unread, categories, mark read/unread/all, pages); My profile → Notifications (type × channel switches, essential types keep one channel, manager-only types hidden, reset) through identity's new `profile.tab` point; Settings → Notifications (types on/off, default channels, reminder lead time, summary time); Settings → Announcements (everyone / roles / a manager's team, publish now/later/draft, end, delete, "read by x of y") with a banner (`app.banner`) and `/announcements`. Identity `findMembers` gained role/scope filters and `listRoleOptions`; leads exports `listLeadSummaries`; assignment exports `getAssignmentSettings`; platform `forEachOrganization` for per-tenant cron jobs. **Found while verifying:** the bell kept its old count after marking on the page (now re-syncs on server refresh and a window event) and a controlled time input lost what was typed before hydration (settings form now reads inputs on submit). **Verified:** lint, format, typecheck, 215 unit + integration tests (13 new for M06: preferences, grouping, center, e-mail delivery, reminders, events, digest time zones, alerts, announcements), 49 E2E tests (5 new) run twice, production build, worker bundle boots with all jobs/handlers, browser runs as admin/manager/executive on desktop and tablet, e-mails checked in Mailpit. | `feat(M06)` |
| 2026-09-25 | M05 | M05-01 → M05-13 | **M05 complete — lead assignment, reassignment & team workload.** Schema + migration (`lead_assignments` history with kind/method/reason/rule and open/ended periods, `reassignment_reasons` master seeded with 7 categories, `assignment_rules`, `leads.owner_assigned_at`; existing owners backfilled as the first history record). **Services:** assign an unassigned lead (New → Assigned automatically), reassign with a mandatory reason (+ optional category) or return a lead to the queue (Assigned → New), all with scope checks (`leads.assign` / `leads.reassign`: TEAM = the actor's reporting tree, OWN = themselves), eligibility (active members whose role can see leads), **optimistic concurrency** (the change only applies if the lead still has the owner the person saw), history + timeline + audit + event (`lead.assigned`, `lead.reassigned`, `lead.unassigned`) in one transaction; bulk assignment sharing leads in turn with a per-lead result; **ownership on creation** decided inside the creating transaction through a new server extension point (`lead.created` hook): the owner chosen on the form or in an import's owner column (checked like a manual assignment), else the creator when they only see their own leads (setting), else the first matching **assignment rule** (channel/source/campaign/project criteria, round robin or least loaded, rule row locked so concurrent imports/API calls take turns fairly), else the unassigned queue; team workload counts (open, not contacted, unworked > N hours since assignment, per status category); "hand over & deactivate" moving a leaving member's open leads to colleagues in turn (or the queue) before deactivating them. **Screens:** Assign/Reassign dialog in the lead header and an **Assignments** tab with the full history (who/when/why/how), **Assign** in the lead list's bulk bar, **Assign to** on the new-lead form, **Unassigned** queue (oldest first, "over N h" flag, per-row and bulk assign), **Team workload** board with drill-down links (new list filters: open only, unworked N h), **Settings → Lead assignment** (rules, reasons, options), **Hand over leads & deactivate** on a user's page. Platform: typed server extension points (`ServerExtensionMap`), a client UI registry (`src/modules/registry.client.ts`) so manifests stay plain data for the worker and the seed, identity `findMembers`, leads `setLeadOwner` / lead-count APIs; the sidebar now highlights only the most specific item. **Fixes found while verifying:** the first manifest version imported a client component, which broke the seed and would have broken the worker (moved to the client UI registry); nested nav items highlighted twice; the rule dialog's strategy select overflowed. **Verified:** lint/format/typecheck clean; production build + worker bundle (boots, no client code inside); 200 unit + integration tests (11 new: creator ownership, team-only assignment, reason required and history kept, concurrent reassignment (one wins), queue return, bulk in turn with skipped leads, round-robin fairness under 6 simultaneous creations, least loaded, form assignee, workload & unworked drill-down, handover & deactivation, creator setting; M04 lead test updated for the self-assignment); 44 E2E tests (new: queue assign → reassign with reason → history, bulk share, workload drill-down + nav highlight, admin assign-on-create + rules, executive has no assignment controls; M05 pages in the console-error sweep); manual browser runs as admin, manager and executive on desktop and tablet. | `feat(M05): lead assignment, reassignment and team workload` |
| 2026-09-25 | M04 | M04-15, M04-16, M04-18 → M04-20, M04-22, M04-23 | **M04 complete — import, export and intake API (part 2 of 2).** **Import:** upload a CSV or Excel file (≤ 10 MB, ≤ 10,000 rows; encoding and separator detected) → columns matched automatically by their names (portal exports and the CRM's own export map themselves) → every row checked with the chosen mapping (errors per row, matches with existing leads and repeats inside the file) → a background job creates the leads **as the person who started the import** (their permissions, data scope and the duplicate policy apply; masters matched by name or code; owner by e-mail or employee code; optional "skip existing customers") → report with a progress bar, counts and the rows not imported, downloadable as a CSV with the reason in front, import history, "view imported leads" filter. The job counts each row in the same transaction as its lead, so a retried job resumes exactly after the last row; imports stop cleanly if the importer is deactivated. **Export:** CSV or Excel of the list as filtered on screen or of the selected rows, within the data scope, audited, generated on request (never stored), columns matching the import template, protected against spreadsheet formula injection. **Intake API** `POST /api/v1/leads`: API keys (shown once, stored as SHA-256 hash, revocable, last use shown), per-key rate limit, `Idempotency-Key` replay for 24 h, per-field validation errors, source/campaign/project/configuration codes, duplicate policy (409 on block), leads recorded with the key as actor; **Settings → API keys** with create/copy/revoke and the API documentation (org URL, fields, responses, source codes). Platform: `src/platform/api` (keys, idempotency, PostgreSQL rate limiting, error shape; ADR 0005), member context for background work on behalf of a person, hourly cleanup job. Also: mobile numbers with fewer than 6 digits are refused everywhere; timeline wording "created from an import / via the intake API". **Fixes found while verifying:** idempotency keys with spaces were refused; the API key table hid the revoke button on narrower screens; review counts read "3of 6" to screen readers. **Verified:** lint/format/typecheck clean; production build + worker bundle (exceljs checked inside the bundle); 188 unit + integration tests (new: CSV/XLSX parsing incl. ANSI files, formula guard, column matching; import preview/run/report/resume/skip/deactivated importer, export scope/format/audit, API auth, idempotency, validation, duplicate policy, rate limit, revocation); 39 E2E tests (new: CSV import through the worker, export download, API key → API lead → revoke, executive has no access; new pages in the console-error sweep); manual browser runs as admin and executive on desktop and tablet. | `feat(M04): lead import, export and intake API` |
| 2026-09-25 | M04 | M04-01 → M04-14, M04-17, M04-21 | **M04 lead management core (part 1 of 2).** Schema + migration (`pg_trgm` GIN indexes on name/e-mail/mobile) for leads, statuses, sources, campaigns, status history, notes, attachments, timeline activities, project interests, saved views (+ tables for import batches, API keys and idempotency used next); per-organization lead numbers (`LD-000001`); masters seeded per organization (15 PRD statuses with category, closed flag, colour and "reason required"; 11 sources). Services with audit + timeline + domain events in one transaction: create/update (timeline `UPDATED` lists changed fields with previous values), mobile numbers normalized to E.164 with the organization's country, duplicate detection on mobile/alternate mobile/e-mail under the organization policy (flag by default, block or allow), status changes with rules (reason where required, workflow statuses only with `leads.status_override`, reopening only with `leads.reopen`), notes (edit own, pin, soft delete), attachments (presigned S3, soft delete), duplicate review (not a duplicate / mark as duplicate / merge moving notes, files, activities and interests), personal and shared saved views, bulk status change lead by lead. Data scope in every query: OWN, TEAM (+ unassigned) or ALL. Screens: **Leads** list (role-based views, search by name, e-mail, lead number or mobile in any format, 13 URL filters with chips, column chooser, saved views, row selection with bulk status change), **New/Edit lead** (live duplicate warning, budgets typed as "80 L"/"1.1 Cr", projects of interest with interest level, first note), **lead detail** (header actions, customer/requirement/projects (M03 quick-info drawer)/record cards, timeline with type filter, notes, attachments, status history, duplicate banner, panel registry for M05+), **Duplicates** queue, **Settings → Lead settings** (statuses, sources, campaigns, duplicate policy). Platform: UI panel registry, `client.ts` module entry for shared client components, reference checks (catalogue records used by leads cannot be deleted). **Fixes found while verifying:** the live duplicate check never ran (the form field's blur handler replaced it); saved views could not be opened with the keyboard and dropped hidden columns; the temperature filter chip showed the raw value; "0 seconds ago" now reads "just now". **Verified:** lint/format/typecheck clean; 165 unit + integration tests (15 lead: numbering & normalization, validation, duplicate policies, merge, scope per role, phone-format search, filters, status rules, bulk changes, field diffs, notes, attachments in scope, protected masters, project reference check, saved views & soft delete); 35 E2E tests (new: executive capture → note → attachment → status with required reason → closed-lead rules → manager reopens; live duplicate warning → merge → search in another phone format; filters → saved view → bulk status; lead settings access; M04 pages in the console-error sweep); manual browser runs as executive, manager and admin on desktop and tablet. | `feat(M04): lead management core` |
| 2026-09-25 | M03 | M03-01 → M03-14 | **M03 complete — builder & project catalogue.** Schema + migration for builders, builder contacts, projects, configurations, amenities, property types, project/builder documents and the three masters (seeded per organization: 7 property types, 12 configurations, 15 amenities); per-organization codes (`BLD-0001`/`PRJ-0001` or typed), decimal prices with a project range derived from its configurations, date-only launch/possession values. Services with audit entries and `builder.created/updated`, `project.created/updated` events; activate/deactivate (optionally cascading to a builder's projects), delete only for unreferenced records via a new server-registry **reference check** that later modules (leads) plug into. Screens: **Builders** (search, status filter, project counts; detail with overview, contacts with one primary, projects, documents, placeholders for leads/performance), **Projects** (filters by builder, city, status, property type, configuration, budget range incl. "85 L"/"1.2 Cr" input, inactive toggle, quick-info drawer), sectioned **project form** (basics, location, launch & possession, repeatable configurations with live price range, facts, amenities, description & highlights), **project detail** (key facts, configurations & pricing, image gallery, documents), **Settings → Project catalogue** (property types, configurations, amenities). Documents upload straight to S3 with presigned URLs; internal documents are visible only to document managers and builder documents default to internal. Permissions `builders.view/manage`, `projects.view/manage`, `projects.files.manage` (view granted to managers and executives). Demo builders and projects in the development seed. Also: exact decimal helpers (`compareDecimals`, `parseAmountInput`), calendar-date formatting, compact prices with two decimals (₹1.65Cr), shared `UrlTabs`, `ConfirmDialog.onOpen`. **Verified:** lint/format/typecheck clean; 143 unit + integration tests (16 catalogue: codes & per-org uniqueness, tenant isolation, contacts, filters incl. budget overlap, validation, audited diffs, cascade deactivation, delete rules, document visibility & download authorization, permission gating); 31 E2E tests (new: admin builder → contact → project → document → status flow, budget filter + quick view, masters, executive read-only; console-error sweep of every M01–M03 page); manual browser runs on desktop and tablet (executive downloads the shared brochure from S3, cannot see internal documents). | `feat(M03): builder and project catalogue` |
| 2026-09-24 | M02 | M02-01 → M02-21 | **M02 complete — identity, access control & team structure.** Better Auth (e-mail + password, database sessions 7 days with daily rolling refresh, httpOnly `crm.*` cookies, no public sign-up, cookie cache off so revocations apply at once); `User`/`Session`/`Account`/`Verification`/`RateLimit` + `Role`/`RolePermission`/`Membership` (status, reporting line, employee code, designation, avatar) with the active organization stored on the session; login/logout/forgot/reset pages, route protection in `proxy.ts` plus server checks in every page and action; password policy, per-IP rate limits and 15-minute lockout after 5 failures; security events (login, failed login, lockout, logout, reset, invitation accepted, password changed, sessions revoked) in the audit log. Permission catalogue from module manifests, system roles seeded once per permission (admin locked to everything), `PermissionSet.has/assert` + `<Can>` + permission-aware navigation, OWN/TEAM/ALL data scopes with a recursive-CTE reporting tree. Screens: **Settings → Users** (search, role/status/manager filters, invite with e-mailed set-password link, edit, role/manager change with cycle and last-admin checks, deactivate/reactivate, password reset, resend invitation, recent activity), **Roles & permissions** (permission matrix with scopes, custom roles copied from others), **My team** (collapsible reporting tree + list; managers see their own tree read-only), **My profile** (details, photo, work details, change password, signed-in devices with sign-out, notification/activity placeholders), **Audit log** (filters by person, record, action and date; before/after view). Also: in-shell 404/403 pages, `RelativeTime`, UUID-safe URL filters, tenant-client policy for global auth models. **Fix found while verifying:** the identity server module was not registered (avatar uploads would fail). **Verified:** lint/format/typecheck clean; 97 unit + integration tests (20 identity: roles, scopes, hierarchy, cycles, last-admin, deactivation, sessions, audit); 27 E2E tests (setup per persona; login/next, logout, 401 API, forgot/reset single-use, role-based navigation for admin/manager/executive, invite → accept → sign in → deactivate → blocked, plus the M01 suite behind login); manual browser runs of every screen on desktop and tablet with no console errors. | `feat(M02): identity, access control and team structure` |
| 2026-09-24 | M01 | M01-18, M01-25 → M01-28 | **M01 complete.** Playwright E2E suite (15 tests, desktop + tablet: smoke/security headers/health/404, organization profile save + validation, logo upload/remove, test e-mail, event-log search/sort/date filter, navigation incl. persisted sidebar state); GitHub Actions CI (lint, format, typecheck, `pnpm audit`, Vitest, build, E2E on real PostgreSQL/RustFS/Mailpit, Docker builds); Dockerfile with `web` (standalone, ~340 MB), `worker` (single esbuild bundle, ~250 MB) and `migrate` targets; README setup guide, ADRs 0001–0004, deployment and backup/restore runbooks. **Fixes found while verifying:** server layout read a constant exported from a client module (sidebar cookie ignored); `SKIP_ENV_VALIDATION` dropped env defaults and crashed the production build; prisma config required `DATABASE_URL` for `generate`; CSP/HSTS now depend on an HTTPS `APP_URL`; audit findings in Prisma CLI transitive deps fixed with overrides. **Verified:** lint/format/typecheck clean, 64 unit + integration tests, 15 E2E tests against both dev and production builds, web and worker containers run against local services (health OK, queued e-mail delivered by the containerized worker). | `feat(M01): e2e tests, CI, Docker images and docs` |
| 2026-09-24 | M01 | M01-14 → M01-17, M01-19 → M01-24 | **Platform services + UI foundation.** S3 storage (presigned PUT/GET, content-type enforcement, CORS) and file service with purposes; SMTP/console/memory e-mail transports + React Email templates + queued delivery; `next-safe-action` pipeline with typed errors; pino logging, request IDs and `/api/health` (DB, storage, worker heartbeat); security headers + nonce-based CSP in `src/proxy.ts`; Dependabot. Tailwind v4 design tokens (light/dark), shadcn-style UI kit, responsive app shell (collapsible sidebar, tablet slide-over), registries (navigation, settings, permissions, extension points), DataTable with URL state (search, sort, paging, filters), DateRangePicker with org-timezone presets, money/date/phone formatters, error/404/403/loading pages. Settings hub, **Organization profile** (company details, logo upload, regional settings, test e-mail) and **System status** (health + domain-event log). **Verified:** 61 unit/integration tests; RustFS + Mailpit integration tests; browser run (Playwright) of save profile → audit + event, logo upload to S3 → sidebar, test e-mail → worker → Mailpit, event-log search/sort/date filter, tablet menu, dark mode, zero console/CSP errors. | `feat(M01): platform services and UI foundation` |
| 2026-09-24 | M01 | M01-01 → M01-13 | **Platform core.** Next.js 16.3 + pnpm + TS strict; ESLint (tenancy/module-boundary import rules), Prettier, Husky, lint-staged, commitlint; `docker-compose.yml` (PostgreSQL 16, RustFS S3 storage, Mailpit); validated env (`src/config/env.ts`, `.env.example`); Prisma 7 multi-file schema + first migration (`organizations`, `organization_settings`, `sequences`, `audit_logs`, `outbox_events`, `file_objects`, `worker_heartbeats`); tenant-scoped Prisma client with guard (T1–T3); per-org sequences; audit log with field diffs; transactional outbox that enqueues handler jobs in the same transaction; pg-boss worker (`pnpm dev:worker`) with cron jobs and heartbeat; storage/e-mail/server-action groundwork; module template; idempotent seed. **Verified:** lint + typecheck clean; 20 integration tests pass (tenant isolation across two orgs, sequence concurrency & rollback, audit diffs & rollback, outbox commit/rollback, worker execution, transactional enqueue, retries). | `feat(M01): platform core` |
| 2026-09-24 | Planning | – | Analysed the PRD (32 sections, 13 pages). Split the web-app scope into 10 modules (M01–M10) with goals, data models, business rules, 183 checklist tasks and acceptance criteria. Defined the architecture (modular monolith on Next.js), multi-tenancy rules T1–T10, access-control model, PRD traceability matrix, future phases (F1 multi-tenant SaaS, F2 mobile, F3 integrations) and risks. Created `BUILD_PLAN.md`, `PROGRESS.md`, `README.md`, `CLAUDE.md`. | initial commit |

---

## 4. Decision Log

| ID | Date | Decision | Reason | Status |
|---|---|---|---|---|
| D-001 | 2026-09-24 | Web app only; the mobile application (PRD §25) is out of scope for now | Requested by product owner | Accepted |
| D-002 | 2026-09-24 | Next.js (App Router) + TypeScript as the application framework | Requested by product owner | Accepted |
| D-003 | 2026-09-24 | Single organization today, multi-tenant-ready data model from day one (shared DB, `organizationId` on every tenant table, rules T1–T10) | Future multi-tenancy requested without data migration | Accepted |
| D-004 | 2026-09-24 | Modular monolith with transport-agnostic services, domain events (outbox) and registries | Progressive, independent module delivery; future mobile API reuse | Accepted |
| D-005 | 2026-09-24 | PostgreSQL + Prisma 7 | Relational integrity, JSONB, `pg_trgm`, RLS path for tenancy | Accepted |
| D-006 | 2026-09-24 | Better Auth for authentication (email + password, DB sessions) | Self-hosted, organization-aware sessions, password reset, rate limiting | Accepted |
| D-007 | 2026-09-24 | pg-boss worker for background jobs (no Redis) | Minimal infrastructure; reminders, digests, imports, exports | Accepted |
| D-008 | 2026-09-24 | Tailwind CSS v4 + shadcn/ui for the UI | Accessible, customizable admin UI components | Accepted |
| D-009 | 2026-09-24 | Build order M01 → M10 (foundation first, features on top) | Each module depends only on earlier ones | Accepted |
| D-010 | 2026-09-24 | Local S3-compatible storage uses **RustFS** instead of MinIO | MinIO community Docker images are no longer published; any S3 API works (AWS S3 / R2 in production) | Accepted |
| D-011 | 2026-09-24 | Event dispatch = outbox row + one pg-boss job per subscribed handler, **enqueued inside the same transaction** (pg-boss `fromPrisma` adapter) instead of a polling relay | Exactly the committed events are dispatched, with per-handler retries and no relay process | Accepted |
| D-013 | 2026-09-24 | The worker ships as one self-contained esbuild bundle (`pnpm build:worker`); migrations/seed run from a separate `migrate` image | Worker image drops from ~3 GB to ~250 MB; the Prisma CLI is only needed for release tasks | Accepted |
| D-014 | 2026-09-24 | Authentication policy: no self sign-up (admins invite; invitation links valid 72 h, reset links 1 h, single use); sessions last 7 days with daily rolling refresh and are validated against the database on every request (no cookie cache); passwords ≥ 10 characters with letters and numbers; 5 failed sign-ins lock the account for 15 minutes; per-IP rate limits on sign-in and password endpoints | Secure defaults for a staff-only CRM; immediate effect of deactivation, password change and "sign out other devices" | Accepted |
| D-015 | 2026-09-24 | The e-mail address is the login identity and cannot be changed in v1 (read-only on the profile and in user administration) | Avoids an unverified identity change; a verified e-mail-change flow can be added later | Accepted |
| D-016 | 2026-09-24 | The Admin role always holds every permission and cannot be edited; system roles can be renamed and re-permissioned but not deleted; custom roles can be deleted only when nobody has them; users are never deleted, only deactivated | Prevents organization lock-out and keeps accountability (PRD §28) | Accepted |
| D-017 | 2026-09-24 | Q-04 default adopted: multi-level reporting hierarchy (any depth, recursive CTE), circular lines rejected; a manager's TEAM scope is their whole subtree | Works for today's single level and for team leads later | Accepted (provisional) |
| D-018 | 2026-09-25 | Builder/project codes are unique per organization: typed codes are kept (uppercase), blank ones come from the org sequence (`BLD-0001`, `PRJ-0001`). A project's price range is not typed but derived from its configurations on every save | Stable references for leads and reports; one source of truth for prices used by budget filters | Accepted |
| D-019 | 2026-09-25 | Documents carry an "internal" flag: internal files are listed and downloadable only with the manage permission (project documents: `projects.files.manage`; builder documents: `builders.manage`). Builder documents are internal by default (agreements, commission terms) | PRD "download permitted documents"; keeps commercial terms away from the sales floor (see Q-16) | Accepted |
| D-020 | 2026-09-25 | Builders/projects/masters are deactivated rather than deleted once anything references them; deletion is allowed only for unreferenced records (added by mistake). Other modules declare references through `ServerModule.referenceChecks` (e.g. M04: leads of a project) | PRD §4 / M03 rule "records with linked leads cannot be deleted" without the catalogue knowing about leads | Accepted |
| D-021 | 2026-09-25 | Proposed defaults for Q-01 (PRD §6 statuses, free transitions; reopening a closed lead needs a permission), Q-02 (flag duplicates on same mobile **or** e-mail, never block), Q-03 (executives may create leads, which are assigned to them) and Q-05 (manual entry + CSV import + intake API) adopted provisionally so M04 can proceed | Product owner asked to keep building; all four are configurable or easy to change later | Accepted (provisional) |
| D-022 | 2026-09-25 | Lead visibility: ALL sees every lead; TEAM sees leads owned by anyone in the reporting subtree **plus unassigned leads**; OWN sees own leads only. Deleted leads are never listed; merged leads only in the Duplicates view. Executives' self-created leads are owned by them (Q-03) | Managers must be able to pick up unassigned enquiries before M05 automates assignment | Accepted (provisional) |
| D-023 | 2026-09-25 | Modules may expose client components through `client.ts` (next to the server-side `index.ts`), and add server-rendered panels to other modules' pages through a UI registry (`src/modules/registry.ui.ts`, e.g. `lead.detail.panel`) | M05–M08 extend the lead page without editing the leads module; client bundles never import server code | Accepted |
| D-024 | 2026-09-25 | Workflow statuses (New / Open, Assigned, Visit, Revisit, Booking, Closed / Won) are set by their workflows (assignment, visits, bookings); setting them by hand needs `leads.status_override` (admins). Reopening a closed lead needs `leads.reopen` (managers). Statuses flagged "reason required" (seeded: Not Interested, Lost, Invalid / Duplicate) need a reason; labels, colours, order and activation are editable, system categories and closed flags are not | PRD §6 status flow stays trustworthy for dashboards while staying configurable (Q-01) | Accepted (provisional) |
| D-025 | 2026-09-25 | Duplicate = same normalized mobile (primary or alternate) or same e-mail among active leads; policy per organization (FLAG default, BLOCK, ALLOW). Merging keeps the existing lead, moves notes, attachments, timeline and project interests, fills its empty fields and marks the other lead merged (Invalid / Duplicate) | Q-02 default; nothing is lost when duplicates are merged | Accepted (provisional) |
| D-026 | 2026-09-25 | Imports run as a background job **acting as the member who started them** (their permissions, scope and the duplicate policy), process each row in its own transaction together with the batch counters (exactly-once resume), accept CSV/XLSX up to 10 MB / 10,000 rows, and never store half-valid rows: rows with errors are reported and downloadable for correction | Large files without request timeouts; no import can do more than its author; accountable results | Accepted |
| D-027 | 2026-09-25 | Public API (`/api/v1`) authenticates with per-organization API keys (hashed, shown once, revocable) that act with a fixed permission set per endpoint; `Idempotency-Key` replays for 24 h; 60 requests/min per key; details in ADR 0005 | Safe lead intake from websites and portals; the same mechanism serves the future mobile app | Accepted |
| D-028 | 2026-09-25 | Exports are generated on request and streamed to the browser (never stored), limited to 50,000 leads and the exporter's data scope, use the import template's columns (re-importable) and neutralise spreadsheet formulas; only admins export by default | Data protection and accountability (every export audited) with a simple operational model | Accepted |
| D-029 | 2026-09-25 | Proposed defaults for Q-06 (manual assignment with optional automatic rules: round robin or least loaded) and Q-08 for M05 (a lead is "unworked" 24 h after assignment without any activity; configurable) adopted provisionally | Product owner asked to keep building; both are settings | Accepted (provisional) |
| D-030 | 2026-09-25 | Ownership rules: the first owner is decided inside the transaction that creates the lead (chosen owner → creator for executives → rules → queue); every change is a `lead_assignments` period with reason, method and actor; assigning moves New → Assigned and unassigning Assigned → New; reassigning needs a reason (≥ 5 characters); only active members whose role can see leads can own leads; managers act within their reporting tree; concurrent changes are rejected optimistically; leaving members hand over their open leads (closed leads keep their historical owner for reporting) | PRD §7 accountability with complete history; no lead is ever silently lost or double-owned | Accepted |
| D-031 | 2026-09-25 | Cross-module extension points come in three kinds: server-rendered UI (`registry.ui.ts`), client components (`registry.client.ts`) and server hooks (`ServerExtensionMap` on server modules); module manifests stay plain data | Manifests are loaded by the worker and the seed, which must never import client or server-action code | Accepted |
| D-032 | 2026-09-25 | Notification types are plain data in module manifests (key, category, default channels, essential, manager-only, e-mail button); the organization turns types on/off and sets default channels, each person chooses channels per type; essential types (new lead for me, reminders, announcements) can never be switched off, only moved between channels; manager-only types are hidden from people who cannot receive them | PRD §18/§21/§26 with "nothing is forgotten" guaranteed for what matters | Accepted |
| D-033 | 2026-09-25 | `notify()` runs in the caller's transaction (in-app row + e-mail delivery + job), idempotency key per recipient, never notifies people about their own actions, groups the events of one bulk request into one counting notification (e-mail after 45 s), managers hear about manual assignments in their team only (not rule/creator ones); the bell polls every 30 s (no SSE yet) | Reliable, spam-free delivery with the existing PostgreSQL job queue (D-007) | Accepted |
| D-034 | 2026-09-25 | Reminders: `scheduled_reminders` rows are the source of truth, keyed by a caller-chosen `dedupeKey` (schedule = create or move, cancel), fired by a delayed job that checks `fireAt` plus a 5-minute sweep (1-minute grace); firing claims the row atomically | Survives worker restarts and lost jobs; reschedules never double-fire | Accepted |
| D-035 | 2026-09-25 | Manager alerts are evaluated hourly but sent only 09:00–20:00 in the organization's time zone, once a day per rule and manager; the daily summary goes to members with a team or organization-wide view of leads, once per local day at the configured time (default 08:30) or within the next 3 hours | Useful, predictable timing without night-time messages | Accepted |
| D-036 | 2026-09-25 | Announcements target everyone, roles or a manager's reporting tree; the audience is notified once when an announcement goes live (now or scheduled); a banner shows until dismissed or expired; reads are counted when the banner is dismissed or `/announcements` is opened; "publish now" tolerates up to 60 s of browser clock skew | PRD §18 announcements with read tracking | Accepted |
| D-037 | 2026-09-25 | Browser web push (M06-13, could-have) is deferred until after v1 | In-app + e-mail meet the acceptance criteria; push needs VAPID keys, a service worker and per-device subscriptions | Accepted |
| D-038 | 2026-09-25 | Proposed defaults for Q-07 (calls are placed from the phone's dialer and logged by hand; recordings are uploaded to the call) and the rest of Q-08 (Unresponsive suggested after 3 unanswered calls in a row, a follow-up is missed 2 h after its time) adopted provisionally | Product owner asked to keep building; both thresholds are settings and a telephony provider plugs in through an adapter | Accepted (provisional) |
| D-039 | 2026-09-25 | Status automation after a call: the outcome's mapped status first, then "first time reached → Contacted", then "N unanswered in a row → Unresponsive"; applied only to open or in-progress leads before the visit stage; a person logging a call sees the suggestion pre-selected and may keep or change it (manual status rules apply); calls reported without a person apply it when the setting is on | Fast disposition without silently moving leads that are further along | Accepted |
| D-040 | 2026-09-25 | Follow-ups and callbacks always belong to the lead's owner: they move with the lead on (re)assignment (setting, on by default) with their reminders, and wait without an owner while the lead is unassigned; someone scheduling for another person notifies them | The person who owns the customer owns the next step (PRD §7, §9) | Accepted |
| D-041 | 2026-09-25 | "Missed" is a state of an open follow-up (overdue beyond the grace period, owner notified once), not a closure: it stays on the agenda until done, moved or cancelled, and `missedAt` is kept when it is done late; rescheduling closes the item as "moved" and creates a linked new one | Nothing is forgotten, and reports can still count missed and late follow-ups (PRD §9, §15) | Accepted |
| D-042 | 2026-09-25 | Call summary (last call, last reached, unanswered in a row, last outcome, next follow-up) is kept on the lead by M07 through a leads API in the same transaction, for list columns, filters and sorting | Lists stay a single indexed query | Accepted |
| D-043 | 2026-09-25 | Other modules add lead list filters through the server extension point `lead.list.filter` (own URL parameter, options per person, Prisma condition); they apply to the list, saved views and exports alike | Extends the lead list without editing its internals (BUILD_PLAN §2.2) | Accepted |
| D-044 | 2026-09-25 | Proposed defaults for Q-09 (stages Booked → Agreement signed → Closed/Won, configurable — "Registration done" seeded but off), Q-10 (credit = the booking's executive: the lead's owner unless another is chosen; it changes only when someone edits it, never on reassignment) and Q-16 (booking values seen and entered only with `bookings.view_value`: admins by default; managers and executives see neither values nor value changes) adopted provisionally | Keeps M08 moving; all three are settings or role permissions | Accepted (provisional) |
| D-045 | 2026-09-25 | Visits and bookings only move a lead **forward** (Visit < Revisit < Booking < Closed/Won) as workflow changes; closed leads get no new visits and lost/invalid leads no bookings until reopened; a won lead may book another unit | Automation never undoes a later stage; reopening stays a deliberate, permissioned act (PRD §6) | Accepted |
| D-046 | 2026-09-25 | A visit is a **revisit** when the lead already has a completed visit (linked to it, same project first); numbering is per kind ("Visit 1, 2…", "Revisit 1, 2…") and a rescheduled visit keeps its number; visits can be recorded up to 30 days back (walk-ins) and then get their outcome | Matches how channel partners count SVs and RVs; history stays connected (PRD §11) | Accepted |
| D-047 | 2026-09-25 | **Loss reasons on every path:** entering a Lost-category status (Lost, Not Interested) needs `leads.mark_lost` and a loss reason that applies to that status — enforced in a leads server hook (`lead.status.changing`) and asked for by a client field extension (`lead.status.fields`) in the status dialog, bulk changes and the call dialog; a lead with an open booking cannot be closed as lost/invalid (cancel the booking instead); closing a lead cancels its upcoming visits; automation (dialer calls) never closes a lead without a reason | PRD §12, §17 require the reason; one rule instead of per-screen checks, without the leads module knowing about deals | Accepted |
| D-048 | 2026-09-25 | Lead milestones for conversion reports: `firstVisitAt` (first completed visit), `bookedAt` (first booking), `closedAt` now means **closed as won** (it meant any closed status — existing rows migrated), `lostAt` + `lossReasonId` while lost / not interested; closed and lost milestones are cleared when the lead is reopened | Visit-to-booking and visit-to-closure conversion (PRD §11) without scanning histories | Accepted |
| D-049 | 2026-09-25 | Booking history is a general `booking_history` (the plan's `BookingStatusHistory`): creation, every edited field with before/after display values, stage moves, closure, cancellation and documents, each with who and when; value changes show as "changed (hidden)" to people without `bookings.view_value`; the audit log keeps the raw values | PRD §12 "retain the history of booking and closure changes" covers more than status | Accepted |
| D-050 | 2026-09-25 | Cancelling a booking (open or closed) needs `bookings.close` and a cancellation reason; when the lead has no other open or won booking it becomes Lost with that reason, or goes back to an active status of the person's choice (default: the status it had before Booking) — reopening a won lead still needs `leads.reopen` | "Lead goes to LOST or back to active (user choice)" (plan), keeping the reopen rule | Accepted |
| D-051 | 2026-09-25 | Upcoming visits move with a reassigned lead (setting, on by default; reminders re-targeted), completed visits keep whoever went; lead lists gain ready-made views through a new `lead.list.preset` manifest point, and saved views now keep other modules' filter parameters (they were dropped before) | Same ownership rule as follow-ups (D-040); "Lost & Not-interested views" (M08-11) without editing the leads list | Accepted |
| D-052 | 2026-09-25 | Proposed defaults for Q-11 and Q-12 adopted: commission from rate cards per builder or project (percentage of the agreement value, flat per deal, or slabs by deal value or by the deal's position in the builder's fiscal year), invoices raised to builders with tax from billing settings (GST 18 % as CGST + SGST within the organization's state, IGST across states, TDS 2 %, SAC 997221) | Product owner asked to keep building; everything is configurable per organization | Accepted (provisional) |
| D-053 | 2026-09-25 | Deal financials are created when a booking is closed as won, from the rate card valid on the booking date (setting: or the closing date), with the project card before the builder card; the numbers are snapshotted, follow booking edits while draft, lock when confirmed (or when an invoice for them is issued), unlock only with a reason and never while billed; a cancelled booking cancels its financials but not its issued invoice (cancel that by hand); bookings closed before billing existed are backfilled on request | Commission must not change silently after the fact; every change is kept in the deal's history and the audit log | Accepted |
| D-054 | 2026-09-25 | Invoices: drafts carry no number and may be discarded; issuing numbers them `PREFIX/2026-27/0001` per fiscal year (the organization's regional fiscal year — billing has no separate one), snapshots seller and bill-to details, and sets the due date from payment terms; issued invoices are cancelled, never deleted, and their number is not reused; the split tax puts the rounding remainder on the second line; payments record amount and TDS together (≤ balance) and drive Issued → Partly paid → Paid; mistaken payments are voided with a reason; an invoice with valid payments cannot be cancelled | Usual Indian invoicing practice; numbers stay gap-free per year except for cancellations | Accepted |
| D-055 | 2026-09-25 | Profit formula: net revenue = gross commission − cashback; net profit = net revenue − sub-broker payout − executive incentive − other deal expenses; GST is a pass-through and TDS a tax credit, so neither is a cost; the P&L is accrual-based on the closing date (not on collections); organization profit = deals' net profit − business expenses of the period; lost-opportunity values are estimates from the customer's budget (upper end) | Matches the PRD's P&L definition (§15, §23) with the tax treatment made explicit | Accepted |
| D-056 | 2026-09-25 | Finance and billing permissions go to admins only; an optional **Accounts** role is offered once per organization (all billing permissions, all bookings with values, builders and projects) and stays deleted if removed; P&L and lost opportunities live under Insights, billing and deal financials in the main navigation; the digest gains a billing block and billing people get a daily overdue-invoice alert; digest lines may show formatted amounts | Q-16 default (D-044) extended to finance; alert and digest reuse M06 | Accepted |
| D-057 | 2026-09-25 | Invoice PDFs are rendered with React-PDF on first download or send and kept in file storage (`billing.invoice`); drafts and cancelled invoices are rendered on request with a watermark and not stored; amounts use the currency code (the standard PDF fonts have no rupee sign) with the total in words for rupee invoices; e-mails are queued with the PDF attached (platform e-mail messages gained base64 attachments); billing registers export to CSV/XLSX through a shared `platform/export` helper | No font files to ship or licence; one stored document per issued invoice | Accepted |
| D-012 | 2026-09-24 | Proposed defaults for Q-13 (INR / Asia/Kolkata / en-IN, stored as org settings), Q-14 (Docker + managed Postgres, SMTP) and Q-15 (daily backups + PITR) adopted provisionally so M01 can proceed | Product owner asked to start building; all three are configuration, easy to change | Accepted (provisional) |

---

## 5. Open Questions

Questions for the business. A module should not start until the questions it needs are answered (or a default is accepted).

| ID | Question | Proposed default | Needed by | Status |
|---|---|---|---|---|
| Q-01 | Final lead status names and any restricted transitions? | PRD §6 list (15 statuses), free transitions except terminal → active needs permission | M04 | Default adopted (provisional, D-021) |
| Q-02 | Duplicate policy: block, flag or allow? Duplicate = same mobile, same email, or both? | FLAG on same mobile **or** email | M04 | Default adopted (provisional, D-021) |
| Q-03 | Can executives create leads, and should self-created leads auto-assign to them? | Yes and yes | M04/M05 | Default adopted (provisional, D-021) |
| Q-04 | Reporting hierarchy: single level (Manager → Executives) or multi-level? | Multi-level supported, single level used initially | M02 | Default adopted (provisional, D-017) |
| Q-05 | Lead sources/portals in use today; does any need API intake at launch? | Manual + CSV import + intake API | M04 | Default adopted (provisional, D-021) |
| Q-06 | Is automatic assignment (round-robin/load-based) needed, or manual only? | Manual in v1, auto rules as should-have | M05 | Default adopted (provisional, D-029) — rules are optional |
| Q-07 | Telephony / call-recording provider (if any)? | Manual call logging + recording upload | M07 | Default adopted (provisional, D-038) — provider adapter ready |
| Q-08 | Thresholds: "unworked" hours, "unresponsive" attempts, missed follow-up grace period | 24 h, 3 attempts, 2 h | M05/M07/M10 | Default adopted (provisional, D-029, D-038) — all three are settings |
| Q-09 | Booking stages between Booking and Closed/Won (agreement, registration…)? When is a deal "closed"? | Booked → Agreement → Closed/Won (configurable) | M08 | Default adopted (provisional, D-044) — stages are a setting |
| Q-10 | Who gets credit for a booking if the lead was reassigned after a visit? | Executive who created the booking | M08/M10 | Default adopted (provisional, D-044) |
| Q-11 | Revenue model & P&L formula (commission basis, slabs, payouts, incentives, cashback)? | % of agreement value per builder/project rate card | M09 | Default adopted (provisional, D-052) — rate cards also allow flat and slab commission |
| Q-12 | Billing workflow: invoices raised to builders? Tax registration & rates, invoice format? | Builder invoices with configurable tax | M09 | Default adopted (provisional, D-052) — GST 18 % (CGST+SGST / IGST), TDS 2 %, all in billing settings |
| Q-13 | Currency, timezone and locale defaults? | INR, Asia/Kolkata, en-IN (org settings) | M01 | Default adopted (provisional, D-012) |
| Q-14 | Hosting preference and email provider? | Docker on a container platform + managed PostgreSQL; SMTP/Resend | M01 | Default adopted (provisional, D-012) |
| Q-15 | Data retention/archival rules and backup RPO/RTO? | Keep all data; daily backups + PITR, RPO 24 h / RTO 4 h | M01 (runbook) | Default adopted (provisional, D-012) |
| Q-16 | Who may see booking values and financial data (managers?) | Admin (+ custom "Accounts" role) only | M08/M09 | Default adopted (provisional, D-044) — `bookings.view_value` |

---

## 6. Known Issues & Technical Debt

| ID | Date | Module | Description | Status |
|---|---|---|---|---|
| KI-001 | 2026-09-24 | M01 | The shadcn/ui registry (ui.shadcn.com) is blocked in the build sandbox, so UI primitives are authored by hand following the shadcn source; `components.json` is included so `pnpm dlx shadcn add` works in a normal environment. | Accepted |
| KI-003 | 2026-09-24 | M01 | `pnpm audit` flagged `mysql2` and `deepmerge-ts` inside the Prisma CLI dependency tree (unused MySQL/config paths). Patched versions are forced via `overrides` in `pnpm-workspace.yaml`; remove them once Prisma ships updated dependencies. | Mitigated |
| KI-004 | 2026-09-24 | M02 | The auth rate limiter and audit IPs trust `X-Forwarded-For`. Production must sit behind a proxy that overwrites the header (see deployment runbook); per-account lockout still applies if it does not. | Mitigated (runbook) |
| KI-005 | 2026-09-24 | M02 | `pg` prints "client.query() when the client is already executing a query is deprecated" — emitted inside Prisma's `@prisma/adapter-pg` transaction executor, not our code. Harmless with pg 8; re-check before pg 9. | Open (upstream) |
| KI-006 | 2026-09-24 | M02 | Pages inside the app stream behind `loading.tsx`, so `notFound()`/`forbidden()` render the right page but with HTTP 200. Fine for an authenticated app; revisit for public pages. | Accepted |
| KI-007 | 2026-09-25 | M04 | The M04 migration creates the `pg_trgm` extension, which needs the database owner (or a superuser) on managed PostgreSQL. | Mitigated (runbook) |
| KI-002 | 2026-09-24 | M01 | `@playwright/test` is pinned to 1.56.1 to match the browsers pre-installed in the build sandbox. Upgrade freely where browsers can be downloaded (CI installs its own). | Open |
