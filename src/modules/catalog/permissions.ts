/** Permission keys of the builder & project catalogue (M03-12). */
export const CATALOG_PERMISSIONS = {
  buildersView: "builders.view",
  buildersManage: "builders.manage",
  projectsView: "projects.view",
  projectsManage: "projects.manage",
  projectFilesManage: "projects.files.manage",
} as const;
