/** Public API of dashboards & reports (M10). Other modules import only from here. */
export type { DashboardWidget, DashboardWidgetProps, ReportCatalogEntry } from "./extensions";
export { analyticsManifest } from "./manifest";
export { ANALYTICS_PERMISSIONS } from "./permissions";
