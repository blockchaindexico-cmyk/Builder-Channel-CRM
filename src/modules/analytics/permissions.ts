/** Permissions of dashboards & reports (M10). */
export const ANALYTICS_PERMISSIONS = {
  /** Reports and dashboard figures — one's own, the team's or everyone's. */
  reportsView: "reports.view",
  /** Download reports as CSV/XLSX. */
  reportsExport: "reports.export",
} as const;
